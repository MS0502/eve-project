name: Exact-head validation bootstrap guard

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  bootstrap-guard:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'M2-D package materialization precedes exact-head validation.'
