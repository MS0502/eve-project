name: A11 assembler probe
on:
  push:
    branches: [fix/habitat-a11-content-addressed-snapshots]
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: echo A11_ASSEMBLER_PROBE
